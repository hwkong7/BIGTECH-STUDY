# 물리 주소와 논리 주소

![image.png](attachment:9a1f279c-b8d3-4490-9a8e-e5e132d3ed0d:image.png)

<aside>
💡

## 물리 주소

- CPU와 프로세스는 메모리의 하드웨어 상 실제 주소
</aside>

<aside>
💡

## 논리 주소

- 프로세스마다 부여되는 0번지부터 시작하는 주소 체계
</aside>

![image.png](attachment:bd182bf1-0f4a-49f5-a7d7-0c40a9b1900c:image.png)

<aside>
💡

## 메모리 관리 장치(이하 MMU(Memory Management Unit))

- CPU와 메모리 사이에 위치, CPU가 이해하는 논리 주소를 메모리가 이해하는 물리 주소로 변환하는 역할
</aside>

# 스와핑과 연속 메모리 할당

![image.png](attachment:ecca3229-32e7-4a3c-8195-46930516e2fd:image.png)

<aside>
💡

## 스와핑

- 메모리에서 사용되지 않는 일부 프로세스를 보조기억장치로 내보내고, 실행할 프로세스를 메모리에 적재하는 메모리 관리 기법
- 프로세스들이 요구하는 메모리 공간 크기 > 실제 메모리 크기
  ![image.png](attachment:1605bb8a-7d14-455d-b250-9845e4272aed:image.png)

</aside>

<aside>
💡

### 스왑 영역

- 보조기억장치의 일부인 영역
- 대기 상태가 되었거나 오랫동안 사용되지 않은 프로세스가 있는 영역
</aside>

<aside>
💡

### 스왑 아웃 (메모리 → 디스크)

- 현재 실행되지 않는 프로세스가 메모리에서 스왑 영역으로 옮겨지는 것
</aside>

<aside>
💡

### 스왑 인 (디스크 → 메모리)

- 스왑 영역에 있는 프로세스가 다시 메모리를 옮겨오는 것
</aside>

# 연속 메모리 할당과 외부 단편화

연속적인 메모리 공간을 할당하는 방식 : 연속 메모리 할당

- 효율적인 방법 X ⇒ 외부 단편화라는 문제를 내포하기 때문

<aside>
💡

## 외부 단편화

- 연속적으로 할당되는 환경에서는 프로세스의 실행과 종료를 반복하며 메모리 사이 사이에 빈 공간이 생긴다. 이때 큰 프로세스를 적재하기 어려운 상황을 초래하고, 이는 메모리 낭비로 이어진다 이러한 상황

![image.png](attachment:d4c9af53-1ff5-4049-82fa-ce9d24ec66d0:image.png)

</aside>

## 페이징을 통한 가상 메모리 관리

이러한 문제를 해결하는 운영체제의 메모리 관리 기술이 바로 **가상 메모리**

<aside>
💡

## 가상 메모리

- 실행하고자 하는 프로그램의 일부만 메모리에 적재해, 실제 메모리보다 더 큰 프로세스를 실행할 수 있도록 만드는 메모리 관리 기법
- 가상 메모리 기법으로 생성된 **논리 주소 공간은 가상 주소 공간**
</aside>

---

<aside>
💡

## 페이징

- 프로세스의 논리 주소 공간을 페이지라는 일정한 단위로 나누고, 물리 주소 공간을 페이지와 동일한 크기의 프레임이라는 일정한 단위로 나눈 뒤 페이지를 프레임에 할당하는 가상 메모리 관리 기법

![image.png](attachment:b149e520-9f59-43ce-b392-74515cffa3e8:image.png)

</aside>

<aside>
❗

불연속적으로 배치 ⇒ 외부 단편화 X

</aside>

<aside>
💡

**스왑 아웃 = 페이지 아웃, 스왑 인 = 페이지 인**

![image.png](attachment:62f3353c-3434-414c-b348-0664c078100c:image.png)

</aside>

---

<aside>
💡

#### 세그멘테이션(segmentation)

- 프로세스를 일정한 크기의 페이지 단위가 아닌 가변적인 크기의 세그먼트(segment) 단위로 분할하는 방식
- 세그먼트는 코드 영역(의 일부) or 데이터 영역(의 일부)
- 외부 단편화 O
</aside>

---

<aside>
💡

## 페이지 테이블

- 물리 메모리 내에 페이지가 불연속적으로 배치되어 있다면 CPU 입장에서는 다음으로 실행할 페이지의 위치를 찾기가 어렵다.
- 이를 해결하기 위해 프로세스의 페이지와 실제로 적재된 프레임을 짝지어주는 정보
- 페이지 번호와 실제로 적재된 프레임 번호가 대응되어 있다.

![image.png](attachment:f4d7a96f-e6d7-4bde-a91e-27ac63acf4f8:image.png)

</aside>

### 테이블 엔트리

- 페이지 테이블을 구성하고 있는 각각의 행들
- 포함되는 정보
  - 페이지 번호
  - 프레임 번호
  - **유효 비트**
  - **보호 비트**
  - **참조 비트**
  - **수정 비트**

<aside>
💡

#### 유효 비트

- 해당 페이지에 접근이 가능한지 여부를 알려 주는 매우 중요한 정보
- 현재 페이지가 메모리 아니면 보조기억장치에 적재되어 있는지 알려 주는 비트 - 1 → 메모리에 있음 - 0 → 디스크에 있음
</aside>

<aside>
💡

#### 페이지 폴트(page fault)

- CPU가 메모리에 적재되지 않은 페이지, 즉 유효 비트가 0인 페이지에 접근하려고 하면 생기는 예외
- 필요한 페이지가 메모리에 없는 경우

#### 처리 과정

1. 기존 상태 저장
2. 페이지 디스크에서 가져오기
3. 메모리에 적재
4. 다시 실행
</aside>

<aside>
💡

#### 보호 비트

- 페이지 보호 기능을 위해 존재하는 비트
- r / w / x 으로 페이지에 접근할 권한을 제한
</aside>

<aside>
💡

#### 참조 비트

- CPU가 해당 페이지에 접근한 적이 있는 지의 여부를 나타내는 비트
- 페이지에 적재한 이후에 **CPU가 읽거나 쓴 페이지**는 참조 비트가 **1**로 설정되고,
- 적재한 이후에 **한번도 읽거나 쓴 적 없는** 페이지는 **0**으로 유지된다.
</aside>

<aside>
💡

#### 수정 비트 = 더티 비트

- 해당 페이지에 데이터를 쓴 적이 있는 지의 여부를 알려 주는 비트
- 수정 비트가 **1이면 변경된 적이 있는 페이지**, **0이면 변경된 적이 없는 페이지**(한번도 접근한 적 없거나 읽기만 했던 페이지) - 1이면) 수정 내역을 보조기억장치에도 반영해두어야하므로 보조기억장치에 대한 쓰기 작업이 필요하다 - 0일 경우) 보조기억장치에 반영할 수정 내역이 없으므로 페이지를 메모리에서 삭제하기도 한다.
</aside>

<aside>
💡

## 내부 단편화

- 모든 프로세스의 크기가 페이지의 배수가 아니라는 것!
- 페이지 하나의 크기보다 작은 크기로 발생하게 되는 메모리 낭비
</aside>

<aside>
💡

### 페이지 테이블 베이스 레지스터(이하 PTBR(Page Table Base Register))

- 특정 프로세스의 페이지 테이블이 적재된 메모리 상의 위치를 가리키는 특별한 레지스터
- 프로세스마다 가지는 정보이므로 각 PCB에 기록, 다른 프로세스로의 문맥 교환이 발생할 때 변경된다.

![image.png](attachment:4918053b-7da6-4a18-b054-d4c3d0bfa05f:image.png)

</aside>

## 문제상황

1 메모리 접근 횟수

- 모든 프로세스의 페이지 테이블이 메모리에 적재되어 있을 경우, CPU는 페이지 테이블에 접근하기 위해 한 번, 실제 프레임에 접근하기 위해 한 번, 이렇게 총 두 번 메모리에 접근해야 한다. ⇒ 메모리에 접근하는 시간이 두 배로 늘어날 수 있다.
- ⇒ 이를 해결하기 위해 TLB(Translation Look-aside Buffer)라는 페이지 테이블의 캐시 메모리가 사용된다.
- TLB는 페이지 테이블의 캐시이므로 참조 지역성의 원리에 근거해 자주 사용할 법한 페이지 위주로 페이지 테이블의 일부 내용을 저장한다.

2 메모리 용량

계층적 페이징(hierarchical paging)

- 페이지 테이블을 페이징하는 방식으로,
- 여러 단계의 페이지를 둔다는 점에서 다단계 페이지 테이블(multilevel page table)기법이라고도 부름

## 페이지 테이블과 효율화 (TLB & 계층적 페이징)

### 1️⃣ 메모리 접근 횟수 - TLB (Translation Look-aside Buffer)

- 페이지 테이블이 메모리에 있기 때문에, 데이터 접근을 위해 메모리를 두 번(테이블 접근 + 실제 데이터 접근) 가야 합니다.
- **해결✅**
  - 자주 쓰는 페이지 변환 정보를 담는 **캐시 메모리인 TLB**를 사용하여 메모리 접근 횟수를 줄입니다.

![image.png](attachment:44efdb91-ab78-4ef6-bcba-1d029de72ad1:image.png)

- TLB 히트(TLB hit)
  - CPU가 접근하려는 논리 주소의 페이지 번호가 TLB에 있을 경우, TLB는 CPU에게 해당 페이지 번호가 적재된 프레임 번호를 알려준다.
  - 이 경우에는 한 번만 메모리에 접근하면 된다
- TLB 미스(TLB miss)
  - 하지만 페이지 번호가 TLB에 없는 경우에는 어쩔 수 없이 페이지가 적재된 프레임을 알기 위해 메모리 내의 페이지 테이블에 접근하는 수밖에 없다.
  - 이 경우에는 추가적인 메모리 접근이 필요
- ⇒ 메모리 접근 횟수를 낮추려면 TLB 히트율을 UP

### 2️⃣ 메모리 용량 - 계층적 페이징 (Hierarchical Paging)

- 프로세스가 커지면 페이지 테이블 자체도 너무 커져 메모리를 낭비합니다.
- **해결✅**
  - 페이지 테이블 자체를 다시 페이징하여 여러 단계로 둡니다. 당장 필요 없는 하위 테이블은 메모리에 올리지 않아도 되므로 용량이 절약됩니다.

![image.png](attachment:54490c9f-61d7-48ed-ba0e-751acfc30d86:image.png)

> 페이지 테이블의 계층은 3개, 4개 그 이상으로도 구성될 수 있다.

- 모든 페이지 테이블을 항상 메모리에 유지할 필요가 없어진다.
- CPU와 가장 가까이 위치한 페이지 테이블(Outer 페이지 테이블)만 메모리에 유지하면 잘린 페이지 테이블의 일부가 보조기억장치에 있더라도 Outer 페이지 테이블을 통해 언제든 접근할 수 있다.

## 페이징 주소 체계

<aside>
💡

### 페이지 번호(page number)

- 몇 번째 페이지 번호에 접근할지를 나타낸다.
</aside>

<aside>
💡

### 변위(offset)

- 접근하려는 주소가 페이지(프레임) 시작 번지로부터 얼만큼 떨어져 있는지를 나타내는 정보
</aside>

<페이지 번호, 변위>로 이루어진 논리 주소는 페이지 테이블을 통해 물리 주소 <프레임 번호, 변위>로 변환된다.

# 페이지 교체 알고리즘

## 요구 페이징(demand paging)

- 메모리에 필요한(요구되는) 페이지만을 적재하는 기법

#### 1️⃣ CPU가 특정 페이지에 접근하는 명령어를 실행한다.

#### 2️⃣ 해당 페이지가 현재 메모리에 있을 경우 (유효 비트가 1일 경우) CPU는 페이지가 적재된 프레임에 접근한다.

#### 3️⃣ 해당 페이지가 현재 메모리에 없을 경우 (유효 비트가 0일 경우) 페이지 폴트가 발생한다.

#### 4️⃣ 페이지 폴트가 발생하면 페이지 폴트 처리 루틴을 통해 해당 페이지를 메모리에 적재하고, 유효 비트를 1로 설정한다.

#### 5️⃣ 다시 1의 과정을 수행한다.

## 순수 요구 페이징(pure demand paging)

- 아무런 페이지도 메모리에 적재하지 않은 채 무작정 프로세스를 실행할 수 있는 기법
- 첫 명령어를 실행하는 순간부터 페이지 폴트가 발생 → 실행에 필요한 페이지가 어느 정도 적재된 이후부터는 페이지 폴트의 발생 빈도가 떨어진다.

# 페이지 교체 알고리즘

- 요구 페이징을 통해 페이지들을 메모리에 점차 적재하다 보면 언젠가는 메모리가 가득 찰 수 있다. 메모리에 페이지가 가득 찬 상황에서 추가적으로 페이지를 적재해야 한다면 메모리에 적재된 일부 페이지를 스왑 아웃 해야한다. 이때 메모리에 적재된 페이지 중 보조기억장치로 내보낼 페이지를 선택하는 방법
- 컴퓨터 전체 성능과 직결된다
- 어떤 페이지 교체 알고리즘이 사용되느냐에 따라 페이지 폴트의 발생 빈도가 달라질 수 있기 때문에
- 좋은 페이지 교체 알고리즘은 메모리에서 사용되지 않을 페이지를 적절하게 보조기억장치로 내보내기 때문에 페이지 폴트의 발생 빈도가 적지만, 나쁜 페이지 교체 알고리즘은 머지않아 사용될 페이지를 보조기억장치로 내보내 페이지 폴트를 자주 발생시킬 수 있다.
- 빈번한 페이지 폴트의 발생은 보조기억장치로부터 필요한 페이지를 가져와야 하는 시간만큼 성능 저하를 가져온다

# 페이지 교체 알고리즘

## 1️⃣ FIFO 페이지 교체 알고리즘

- 먼저 들어온 페이지 제거

![image.png](attachment:bc503043-c27b-4ea0-9357-60c994b70e79:image.png)

❌ 단점

- 자주 쓰는 페이지 제거 가능

---

## 2️⃣ 최적(Optimal) 페이지 교체 알고리즘

- 미래에 가장 안 쓸 페이지 제거

![image.png](attachment:8bf754f2-d63b-4d13-b6f2-5199027171a4:image.png)

✔ 장점

- 최적 성능

❌ 단점

- 구현 불가능

---

## 3️⃣ LRU(Least-Recently-Used) 페이지 교체 알고리즘⭐

- 가장 오래 안 쓴 페이지 제거

![image.png](attachment:9300b53f-3405-442b-9309-d91d5cddbc68:image.png)

---

<aside>
💡

#### 스래싱(thrashing)

- 프로세스가 실제로 실행되는 시간보다 페이징에 더 많은 시간을 소요하여 성능이 저하되는 문제
</aside>

---

### 페이지 폴트의 종류

#### 메이저 페이지 폴트(major page fault)

- 보조기억장치에서 CPU가 원하는 페이지를 읽어 들이기 위해 입출력 작업이 필요한 페이지 폴트
- CPU가 접근하려는 페이지가 물리 메모리에 없을 때 발생하는 페이지 폴트
- 디스크 접근 필요 → 느림

#### 마이너 페이지 폴트(minor page fault)

- 보조기억장치와의 입출력이 필요하지 않은 페이지 폴트
- CPU가 요청한 페이지가 물리 메모리에는 존재하지만, 페이지 테이블 상에는 반영되지 않은 경우에 발생할 수 있다.
- 메모리에 있음 → 빠름
- 메이저 페이지 폴트에 비해 성능 상의 악영향이 적다.
