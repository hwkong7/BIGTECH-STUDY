## RAM

> 휘발성 저장장치
> 
- RAM의 용량이 작으면 보조기억장치로부터 실행할 프로그램을 가지고 오는 일이 잦아져 실행 시간이 길어짐.
- RAM의 용량이 충분히 크면 보조기억장치로부터 많은 데이터를 가져와 미리 RAM에 저장할 수 있기 때문에 많은 프로그램을 동시에 실행하는 데 유리함.
- RAM의 용량이 필요 이상으로 커진다고 해서 반드시 컴퓨터의 성능이 그에 비례하여 향상되는 것은 아님
- RAM은 임의 접근 메모리
    - 임의 접근: 저장된 요소에 순차적으로 접근할 필요 없이 임의의 위치에 곧장 접근 가능한 방식
        - 따라서 1번지에 접근하든 100번지에 접근하든 데이터에 접근하는 시간 동일
        
        ![image.png](attachment:7a2afdf4-a461-46b4-b28c-d6eb521cd747:ade6261d-6cc2-4d6e-b39f-44e2152e3749.png)
        
    - 순차 접근: 특정 위치에 저장된 요소에 접근하기 위해 처음부터 순차적으로 접근하는 방식
        
        ![image.png](attachment:1ca91a5c-503e-4573-a556-b709e67c0d2a:24975aa5-89aa-4455-88c6-e9bb072c9c63.png)
        

---

## RAM의 종류

- DRAM(Dynamic RAM)
    - 시간이 지나면 저장된 데이터가 점차 사라지는 RAM
    - 데이터의 소멸을 막기 위해 일정 주기로 데이터를 다시 저장해야 함.
    - 일반적으로 DRAM을 메모리로 사용 → 비교적 DRAM의 소비 전력이 낮고, 저렴하며, 집적도가 높아 메모리를 대용량으로 설계하기 용이하기 때문
- SRAM(Static RAM)
    - 시간이 지나도 저장된 데이터가 사라지지 않는 RAM
    - 저장된 데이터가 사라지지 않는다고 해서 SRAM이 비휘발성 저장장치는 아님
        - SRAM도 전원이 공급되지 않으면 저장된 내용이 소실됨.
    - DRAM과 비교해 속도는 빠르지만, 소비 전력이 크고 가격도 비싸며 집적도도 낮기 때문에 캐시 메모리 등에서 사용
- SDRAM(Synchronous Dynamic RAM, SDR SDRAM(Single Data Rate SDRAM))
    - 클럭 신호와 동기화된, 보다 발전된 형태의 DRAM
    - 클럭 신호와 동기화됨 == 클럭 타이밍에 맞춰 CPU와 정보를 주고받을 수 있음
- DDR SDRM(Double Data Rate SDRAM)
    - 대역폭을 넓혀 속도를 빠르게 만든 SDRAM
    - SDRAM → 한 클럭당 한 번씩 CPU와 데이터를 주고받을 수 있음.
    - DDR SDRAM → 한 클럭당 두 번씩 CPU와 데이터를 주고받을 수 있음.
    - 따라서 DDR SDRAM은 SDRAM보다 전송 속도가 두 배 가량 빠름.

---

## 메모리에 바이트를 밀어 넣는 순서

- 메모리는 CPU로부터 데이터를 **워드** 단위로 받아들이고, 데이터를 **바이트** 단위로 저장 및 관리
- 여러 바이트로 구성된 데이터를 받아들여 여러 주소에 걸쳐 저장
    - ex) 1바이트씩 저장하는 메모리 → 4바이트의 데이터를 4개의 주소에 저장

---

## 빅 엔디안

- 낮은 번지의 주소에 상위 바이트부터 저장하는 방식
- 상위 바이트: 가장 큰 값
    - 123(10진수)에서 가장 큰 수는 100을 나타내는 1 → 1A2B3C4D(16진수)에서 가장 큰 수는 1A
- MSB: 숫자의 크기에 가장 큰 영향을 미치는 유효 숫자(가장 왼쪽에 있는 비트)
- 빅 엔디안은 MSB가 있는 비트, 즉 중요하고 큰 데이터부터 저장해 나가는 방식
- 장점: 우리가 일상적으로 숫자 체계를 읽고 쓰는 순서와 동일하기 때문에 메모리 값을 직접 읽거나, 디버깅할 때 편리

![image.png](attachment:df0f42a2-0d2c-41ac-ad51-7827c6e4a186:image.png)

---

## 리틀 엔디안

- 낮은 번지의 주소에 하위 바이트부터 저장하는 방식
- 하위 바이트: 가장 작은 값
    - 1A2B3C4D(16진수)에서 가장 작은 수는 4D
- LSB: 숫자의 크기에 가장 적은 영향을 미치는 유효 숫자(가장 오른쪽에 있는 비트)
- 리틀 엔디안은 LSB가 있는 바이트, 즉 덜 중요하고 작은 데이터부터 저장해 나가는 방식
- 단점: 메모리 값을 직접 읽고 쓰기 불편함
- 장점: 수치 계산 편리
    - ex) 123 + 456이라는 덧셈을 수행할 때, 가장 작은 값인 일의 자릿수 3과 6부터 계산해 나감

![image.png](attachment:c4a07e01-b4c9-4a81-afcc-11aa8ddf36ea:image.png)

---

## 캐시 메모리

> CPU의 연산 속도와 메모리 접근 속도의 차이를 줄이기 위해 탄생한 저장장치
> 
- CPU가 메모리에 접근하는 속도 < CPU가 레지스터에 접근하는 속도
    
    ⇒ CPU의 연산 속도가 아무리 빨라도 메모리에 접근하는 속도가 느리면 CPU의 빠른 연산 속도는 아무 효용 X
    
- CPU와 메모리 사이에 위치한 SRAM 기반의 저장장치
- 메모리에서 CPU가 사용할 일부 데이터를 미리 캐시 메모리로 가져와 활용
- 일반적으로 L1 캐시와 L2 캐시는 코어 내부에, L3 캐시는 코어 외부에 위치
    - 코어와의 거리: L1 < L2 < L3
    - 캐시 메모리의 크기: L1 < L2 < L3
    - 속도: L1 > L2 > L3
    - L1 캐시 메모리의 경우 명령어만 을저장하는 L1I 캐시와 데이터만을 저장하는 L1D 캐시로 구분되기도 함 ⇒ 분리형 캐시
    
    ![image.png](attachment:cf950a49-f6bd-4797-a277-cba1e09d8997:image.png)
    

---

## 캐시 히트와 캐시 미스

- 보조저장장치 → 전원이 꺼져도 보관할 것을 저장
- 메모리 → 실행 중인 것을 저장
- 캐시 메모리 → CPU가 사용할 법한 것을 저장
- 캐시 적중률: 캐시가 히트되는 비율

**캐시 히트**

- 캐시 메모리가 예측하여 저장한 데이터가 CPU에 의해 실제로 사용되는 겨우

![image.png](attachment:ede21e13-a036-478d-b2b2-2a393bff6493:9707db86-525b-487b-8b77-1310ae2cc072.png)

**캐시 미스**

- 자주 사용될 것으로 예측하여 캐시 메모리에 저장했지만 틀린 예측으로 인해 CPU가 메모리로부터 필요한 데이터를 직접 가져와야 하는 경우

![image.png](attachment:cb0ca2c4-866e-4dd6-99df-2a404d390393:e3392f85-cae6-4117-a69e-d1e3b6d8404f.png)

---

## 참조 지역성의 원리

> CPU가 메모리에 접근할 때 보이는 시간 지역성, 공간지역성의 주된 경향
> 
- CPU는 참조 지역성의 원리에 따라 메모리로부터 가져올 데이터를 결정
- 시간 지역성: CPU는 최근에 접근했던 메모리 공간에 다시 접근하려는 경향이 있다. (ex. 변수)
- 공간 지역성: CPU는 접근한 메모리 공간의 근처에 접근하려는 경향이 있다. (ex. 배열)

---

## 캐시 메모리의 쓰기 정책과 일관성

- CPU가 캐시 메모리에 데이터를 쓸 때는 캐시 메모리에 새롭게 쓰여진 데이터와 메모리 상의 데이터가 일관성을 유지해야 함.
- 문제 상황
    
    ```jsx
    1. 명령어1: 1000번지 값을 300으로 변경 
    2. 명령어2: 100번지 값 출력
    ```
    
    ![image.png](attachment:f157086a-6543-409c-b732-61b42d20e1e3:image.png)
    
- **즉시 쓰기(write-through)**
    - 캐시 메모리와 메모리에 동시에 쓰는 방법
    - 장점: 메모리를 항상 최신 상태로 유지하여 캐시 메모리와 메모리 간의 일관성이 깨지는 상황을 방지할 수 있음.
    - 단점: 데이터를 쓸 때마다 메모리를 참조해야 하므로 버스의 사용 시간과 쓰기 시간이 늘어남.
    
    ![image.png](attachment:6375a569-842d-4463-8a66-fa38ff9ba066:image.png)
    
- **지연 쓰기(write-back)**
    - 캐시 메모리에만 값을 써 두었다가 추후 수정된 데이터를 한 번에 메모리에 반영하는 방법
    - 장점: 메모리 접근 횟수를 줄일 수 있어 즉시 쓰기 방식에 비해 속도 빠름
    - 단점: 메모리와 캐시 메모리 간의 일관성이 깨질 수 있음
    
    ![image.png](attachment:0dcebddb-c5ac-49c4-a336-5c4be2c85e6d:image.png)
