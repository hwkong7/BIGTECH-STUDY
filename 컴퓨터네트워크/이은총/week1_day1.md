## Chap1. OSI 7계층
> 응용 / 표현 / 세션 / 전송 / 네트워크 / 데이터 링크 / 물리
- TCP/IP에서는 이 7계층이 4계층으로 표기된다는 것에 조심해야 한다.
- ``응용/표현/세션`` => 응용 계층
- ``전송`` => 전송 계층
- ``네트워크`` => 인터넷 계층
- ``데이터링크 / 물리`` => 물리 계층

## 캡슐화와 역캡슐화
- 캡슐화는 송신 과정에서 헤더를 추가해 나가는 과정.
- 역캡슐화는 캡슐화 과정에서 붙인 헤더를 각 계층에서 제거해 나가는 과정. 

### Question 1. 빈칸 맞추기
응용 계층은 ``(  1  )``을 전송 계층으로 보낸다. 전송 계층은 ``( 1 )``에 제어 정보를 추가하여 ``( 2 )``를 구성한다. ``( 2 )``는 다시 네트워크 계층으로 보내져 필요한 제어정보가 추가되어 ``( 3 )``을 만든다. ``( 3 )``은 링크 계층으로 전달되고 링크 계층도 자신의 제어 정보를 추가하여 `` ( 4 ) ``를 만든다. 


## Chap2. 물리와 데이터 링크
> 정보보안기사, 학교 중간고사 어디에서도 해당 개념을 출제하지 않았다.

아무래도 컴퓨터공학과 학생이라면 전선을 만질 일이 없다보니, 간단히 스킵한 것으로 보인다. 
간단히 정리하자면 이더넷과 와이파이 개념 정도..?

## Chap3. 네트워크 계층
> 전송 계층만은 아니지만 다음 챕터를 위한 빌드업 챕터.

### Question 1. 
ARP Spoofing과 DNS Spoofing에 대한 설명으로 옳지 **않은** 것은?
1. ARP Spoofing은 MAC 주소를 공격자가 변조하여 속이는 공격 기법이다.
2. ARP Spoofing은 ARP Cache Table이 가짜 MAC 주소로 업데이트하여 공격한다.
3. ARP Spoofing은 arpspoof, DNS Spoofing은 dnsspoof 도구로 공격할 수 있다.
4. DNS Spoofing은 가짜 MAC 주소를 DNS Response한다. 

### Question 2. 
IP 주소 Class에 대한 설명으로 올바르지 **않은** 것은?
1. IP 주소는 A클래스, B 클래스, C클래스, D 클래스로 분류된다.
2. C 클래스는 128~191번의 IP로 가장 앞자리가 10으로 시작한다.
3. 클래스는 네트워크 ID와호스트 ID로 분류된다.
4. A 클래스에서 C 클래스로 갈수록 네트워크 ID가 커지고 호스트 ID는 작아진다. 

## Chap4. 전송 계층
> 아무래도 하이라이트. 모든 문제들은 이곳을 향한다. 아래는 개인 오답노트 및 문제들이다.

교재에서는 학교에서 배운 TCP Slow Start, TCP Reno와 같은 설명은 자세히 되어 있지 않다. 

### Question 1. 
NAT에 대해 설명해 보세요.

### Question 2.
TCP의 MSS는 MTU에 의해 결정되는지 이유와 함께 설명하세요.

### Question 3. 
TCP의 3-way handshaking에 대해 설명해 보세요.